apiVersion: v1
kind: Pod
metadata:
  name: mysecret
  labels:
    name: expence
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - secretRef:
        name: secret
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 8080
