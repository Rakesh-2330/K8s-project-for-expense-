apiVersion: v1
kind: Pod
metadata:
  name: limits
  labels:
    name: nginx
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      limits:
        memory: "100Mi"
        cpu: "120m"
    
